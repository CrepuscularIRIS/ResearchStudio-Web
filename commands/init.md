---
description: Scaffold .research/, the GOAL.md campaign block, and CLAUDE.md for a research-harness campaign in this project
allowed-tools: Bash(python3:*), Read
---
Run `python3 "${CLAUDE_PLUGIN_ROOT}/scripts/init_research.py"` from the project root and show its output. Then tell the user which GOAL.md fields to fill and that `claude mcp add arbor -- arbor mcp` must be registered before phase C.
