---
description: Where the pipeline is — the navigator's next step for a run root (pure function of the ledger, build marks and unit state)
allowed-tools: Bash(python3:*)
---
Run `python3 .research/tools/exp/next.py <run_root>` and show the output verbatim. Do not interpret the science; the emit is the report.
